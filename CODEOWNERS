# Changing another role's operating doctrine should require that role to see it.
# This is the mechanical form of "route it to the owner" — it does not depend on
# anyone remembering to route.

/prompts/      @jobordu
/goals/        @jobordu
/tools/        @jobordu

# grants/ holds AD-HOC AUTHORIZATIONS. Review here is not a courtesy — it is the
# only thing standing between a committed file and a capability, since the checker
# that reads these records authenticates nothing itself (grants/README.md).
/grants/       @jobordu
